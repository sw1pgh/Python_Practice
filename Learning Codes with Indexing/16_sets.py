set_A = {True, "Swap", 19, 1, 3, 2, 5, 5, 5} 
# Even if we repeat a particular value multiple times, when we print, it will be printed only a single time for a Set
# Sets are always printed in a Sorted order and also the data types have a hierarchical order in which they are printed

empty_set = set()

print(f"{set_A}")
print(f"{type(empty_set)}") # Do NOT use a = {} as it will create an empty dictionary and not a Set