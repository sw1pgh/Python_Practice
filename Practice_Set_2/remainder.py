# Program to find the remainder when a divident is divided by a divsor --> All User Inputs
divident = float(input("Please enter the divident number: "))
divisor = float(input("Please enter the divisor: "))

print(f"{divident} / {divisor} leaves {divident % divisor} as the remainder")